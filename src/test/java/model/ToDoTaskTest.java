package model;

import static org.junit.Assert.*;

import java.time.LocalDate;

import org.junit.Test;

public class ToDoTaskTest {
	 
	@Test
	public void testConstructor() {
//		ToDoTask task = new ToDoTask("Task Description", LocalDate.of(2018,01,01), ToDoTask.TaskCatagory.TECHNOLOGY, ToDoTask.TaskPriority.LOW, "Add Details here");
//		assertNotNull(task);
//		assertNotNull(task.getDescription());
//		assertNotNull(task.getCreationDate());
//		assertNotNull(task.getDueDate());
//		assertNotNull(task.getCatagory());
//		assertNotNull(task.getPriority());
//		assertNotNull(task.getDetails());
	}

}
